import 'package:flutter_web_plugins/flutter_web_plugins.dart';

// Web stub — all capabilities return false/null because browsers provide no
// WiFi scanning or current-SSID APIs.
class LayrzWifiWeb {
  static void registerWith(Registrar registrar) {}
}
